<%= link_to "new deck", new_deck_path %>
<% @decks.each do |deck| %>
<div><%= link_to deck.name, root_path %> -
  <%= link_to "edit", edit_deck_path(deck) %>
  <%= link_to "delete", deck_path(deck), method: :delete %>
</div>
<% end %>
